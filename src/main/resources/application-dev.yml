spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cache:
    type: simple

logging:
  level:
    org.example.librarymanagement: DEBUG
    org.springframework.cache: TRACE
